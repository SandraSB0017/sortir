{% extends 'base.html.twig' %}

{% block title %}Mon profil{% endblock %}

{% block body %}
    <h1>Mon profil</h1>

    {{ form_start(editForm) }}
    <div class="photo">
    {% if participant.photo %}
        </div>
    <img src="{{ asset('images/' ~participant.photo)}}" alt="photo de profil">
    {% else %}
        <img src="{{ asset('images/default.jpg.jpg')}}" alt="photo par défaut">
    {% endif %}


    {{ form_end(editForm) }}
    <button class="btn btn-danger" type="button" name="annuler" onclick="history.go(0)">Annuler</button>
{% endblock %}