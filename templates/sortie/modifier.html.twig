{% extends 'base.html.twig' %}

{% block title %}Modifier une sortie{% endblock %}

{% block body %}
    <h1>Modifier une sortie</h1>

    {{ form_start(sortieForm) }}
    {{ form_widget(sortieForm) }}
    <button class="btnValidation btn" type="submit">modifier</button>
    {{ form_end(sortieForm) }}
{% endblock %}