{% extends 'base.html.twig' %}

{% block title %}Annuler une sortie{% endblock %}

{% block body %}
    <h1>Annuler une sortie</h1>

{#   <div class="formAnnulation">#}
{#       {{ form_start(sortieForm) }}#}
{#    {{ form_row(sortieForm.infosSortie) }}#}
{#    <a href="{{ path('app_accueil') }}" type="reset" class="btn btnValidation">Annuler</a>#}
{#    <button type="submit" class="btn btnValidation">Enregistrer l'annulation</button>#}
{#       {{ form_end(sortieForm) }}#}
{#   </div>#}


    {{ form_start(sortieForm) }}

        {{ form_row(sortieForm.nom) }}
        {{ form_row(sortieForm.infosSortie, {
            'label': 'Motif annulation'
        }) }}

        <div class="hidden">
            {{ form_row(sortieForm.dateLimiteInscription) }}
            {{ form_row(sortieForm.dateHeureDebut) }}
            {{ form_row(sortieForm.duree) }}
            {{ form_row(sortieForm.nbInscriptionsMax) }}
            {{ form_row(sortieForm.lieu) }}

        </div>

        <a href="{{ path('sortir_app_accueil') }}" type="reset" class="btn btnValidation">Annuler</a>
        <button type="submit" class="btn btnValidation">Enregistrer l'annulation</button>
    {{ form_end(sortieForm) }}


{% endblock %}